diff --git a/CMakeLists.txt b/CMakeLists.txt
index 92b52f7d1..8117842f9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -80,16 +80,14 @@ if (EMSCRIPTEN)
   set(ZLIB_LIBRARIES)
   set(ZLIB_INCLUDE_DIR)
 
-  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -s ALLOW_MEMORY_GROWTH=1 -s MEMFS_APPEND_TO_TYPED_ARRAYS=1 -s USE_ZLIB=1 -s MODULARIZE=1 \
-    -s EXPORT_NAME=\"'createTdwebModule'\" -s WEBSOCKET_URL=\"'wss:#'\" -s EXPORTED_RUNTIME_METHODS=\"['FS','cwrap']\" -lidbfs.js -lworkerfs.js")
-  set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -s ALLOW_MEMORY_GROWTH=1 -s MEMFS_APPEND_TO_TYPED_ARRAYS=1 -s USE_ZLIB=1 -s MODULARIZE=1 \
-    -s EXPORT_NAME=\"'createTdwebModule'\" -s WEBSOCKET_URL=\"'wss:#'\" -s EXPORTED_RUNTIME_METHODS=\"['FS','cwrap']\" -lidbfs.js -lworkerfs.js")
+  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -s ALLOW_MEMORY_GROWTH=1 -s MEMFS_APPEND_TO_TYPED_ARRAYS=1 -s USE_ZLIB=1 -s MODULARIZE=1 -s EXPORT_ES6=1 -s ENVIRONMENT=web,worker \
+    -s EXPORT_NAME=\"'createTdwebModule'\" --emit-tsd td_wasm.d.ts -s WEBSOCKET_URL=\"'wss:#'\" -s EXPORTED_RUNTIME_METHODS=\"['FS','cwrap']\" -lidbfs.js -lworkerfs.js")
+  set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -s ALLOW_MEMORY_GROWTH=1 -s MEMFS_APPEND_TO_TYPED_ARRAYS=1 -s USE_ZLIB=1 -s MODULARIZE=1 -s EXPORT_ES6=1 -s ENVIRONMENT=web,worker \
+    -s EXPORT_NAME=\"'createTdwebModule'\" --emit-tsd td_wasm.d.ts -s WEBSOCKET_URL=\"'wss:#'\" -s EXPORTED_RUNTIME_METHODS=\"['FS','cwrap']\" -lidbfs.js -lworkerfs.js")
   set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -s DEMANGLE_SUPPORT=1 -s ASSERTIONS=1")
   set(CMAKE_C_FLAGS_DEBUG "${CMAKE_C_FLAGS_DEBUG} -s DEMANGLE_SUPPORT=1 -s ASSERTIONS=1")
 
   set(TD_EMSCRIPTEN td_wasm)
-  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -s WASM=1 --post-js ${CMAKE_CURRENT_SOURCE_DIR}/post.js")
-  set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -s WASM=1")
 endif()
 
 if (NOT TD_GENERATE_SOURCE_FILES)
diff --git a/example/web/build-openssl.sh b/example/web/build-openssl.sh
index fc8063778..377f80775 100755
--- a/example/web/build-openssl.sh
+++ b/example/web/build-openssl.sh
@@ -2,7 +2,7 @@
 cd $(dirname $0)
 
 emconfigure true 2> /dev/null || { echo 'emconfigure not found. Install emsdk and add emconfigure and emmake to PATH environment variable. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html. Do not forget to add `emconfigure` and `emmake` to the PATH environment variable via `emsdk/emsdk_env.sh` script.'; exit 1; }
-emcc --check 2>&1 | grep -q ' 3.1.1 ' || { echo 'emcc 3.1.1 check failed. Install emsdk and install and activate 3.1.1 tools. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html.'; exit 1; }
+# emcc --check 2>&1 | grep -q ' 3.1.1 ' || { echo 'emcc 3.1.1 check failed. Install emsdk and install and activate 3.1.1 tools. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html.'; exit 1; }
 
 OPENSSL=OpenSSL_1_1_0l
 if [ ! -f $OPENSSL.tar.gz ]; then
@@ -20,7 +20,7 @@ sed -i.bak 's/-ldl //g' Makefile || exit 1
 sed -i.bak 's/-O3/-Os/g' Makefile || exit 1
 echo "Building OpenSSL..."
 emmake make depend || exit 1
-emmake make -j 4 || exit 1
+emmake make -j "$(getconf _NPROCESSORS_ONLN)" || exit 1
 
 rm -rf ../build/crypto || exit 1
 mkdir -p ../build/crypto/lib || exit 1
diff --git a/example/web/build-tdlib.sh b/example/web/build-tdlib.sh
index a7ea4d8d7..3f1f8dcf8 100755
--- a/example/web/build-tdlib.sh
+++ b/example/web/build-tdlib.sh
@@ -2,7 +2,7 @@
 cd $(dirname $0)
 
 emcmake true 2> /dev/null || { echo 'emcmake not found. Install emsdk and add emcmake and emmake to PATH environment variable. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html. Do not forget to add `emcmake` and `emmake` to the PATH environment variable via `emsdk/emsdk_env.sh` script.'; exit 1; }
-emcc --check 2>&1 | grep -q ' 3.1.1 ' || { echo 'emcc 3.1.1 check failed. Install emsdk and install and activate 3.1.1 tools. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html.'; exit 1; }
+# emcc --check 2>&1 | grep -q ' 3.1.1 ' || { echo 'emcc 3.1.1 check failed. Install emsdk and install and activate 3.1.1 tools. See instruction at https://kripken.github.io/emscripten-site/docs/getting_started/downloads.html.'; exit 1; }
 
 rm -rf build/generate
 rm -rf build/asmjs
@@ -33,6 +33,6 @@ eval emcmake cmake -DCMAKE_BUILD_TYPE=MinSizeRel $OPENSSL_OPTIONS $TD_ROOT || ex
 cd ../..
 
 echo "Generating TDLib autogenerated source files..."
-cmake --build build/generate || exit 1
+cmake --build build/generate --parallel "$(getconf _NPROCESSORS_ONLN)" || exit 1
 echo "Building TDLib to WebAssembly..."
-cmake --build build/wasm --target td_wasm || exit 1
+cmake --build build/wasm --target td_wasm --parallel "$(getconf _NPROCESSORS_ONLN)" || exit 1
